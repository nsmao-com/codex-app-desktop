# Nice Codex 更新日志

本文件从 1.0.6 版本开始维护。每次发布时在文件顶部新增一个版本，并按“新增、修改、修复”分类记录，可直接作为 GitHub Release 说明的基础。

## 1.4.4 - 2026-08-22

### 新增

- Composer 底部操作区新增统一的容器内响应式布局，单栏与竞技场窄栏复用同一套权限、模式、模型、思考强度、上下文和发送操作。

### 修改

- 输入正文与底部工具栏合并为一个完整圆角卡片；发送/停止按钮改为工具栏最右侧的正常布局元素，不再悬浮覆盖输入区。
- 空草稿的发送按钮改为中性禁用态，只有具备可发送内容或进入发送过程时才显示主操作色。

### 修复

- 修复输入框边框提前结束，导致权限、执行模式、模型和思考强度散落在卡片外的问题。
- 修复展开按钮与发送按钮沿右侧垂直挤压、空白输入区比例失衡，以及窄栏下操作项容易互相覆盖的问题。

## 1.4.3 - 2026-08-22

### 新增

- 对话思考状态正式接入 `thinking-orbs` 官方 Canvas 引擎，使用适合行内展示的呼吸 Orb，并支持明暗主题、高分屏、页面可见性与减少动态效果设置。

### 修改

- AI Loading 只在时间线确实等待回复或消息仍在规划时展示；输入框无论待机、发送还是运行中都不再放置无业务含义的装饰 Orb。
- 输入框展开/收起按钮固定在右上角，发送与停止继续在底部同一位置切换；“跳到最新”只保留导航反馈，不再混用手写加载脉冲。

### 修复

- 修复此前思考动画使用手写圆点和伪元素、没有采用指定官方组件视觉的问题。
- 修复输入框运行时右上角动画挤动展开按钮、在普通输入状态附近形成多余视觉干扰的问题。

## 1.4.2 - 2026-08-22

### 新增

- Grok Build 回合新增原生 `events.jsonl` 结束事件监测，与 CLI 流式 `end` 事件共同确认真实完成、失败或中断状态。

### 修改

- Grok Build 收到原生回合结束信号后会主动收拢本回合 CLI 与 MCP 子进程树，不再把子进程是否自行退出当作消息完成的唯一依据。

### 修复

- 修复 Grok Build 已输出完整回复并写入原生历史后，界面仍长期显示“正在思考中”、停止图标不恢复且后续队列永久卡住的问题。
- 修复 Grok Build 多会话并发、网络波动恢复或工具调用结束时，残留 MCP 子进程延迟终止事件，导致切回会话后运行态与真实历史不一致的问题。

## 1.4.1 - 2026-08-21

### 新增

- Claude Code 与 Grok Build 的首次回合新增原生会话即时绑定：CLI 成功启动后即建立 Nice Codex 会话与原生 transcript 的稳定归属。

### 修改

- 外部模型商会话事件调整为进程成功启动后、读取流式输出前发布，避免启动失败时留下无法恢复的虚假会话。
- Grok 运行中会话即使尚未生成原生 `summary.json`，仍会在侧栏保留可切回、可停止的入口；原生摘要生成后再无缝接管标题与历史。

### 修复

- 修复 Claude Code 首条消息运行时同时出现 Nice Codex 会话和原生 transcript 两个侧栏入口，导致二次消息、停止和队列可能绑定错误的问题。
- 修复 Grok Build 发送途中切换会话或其他会话完成刷新列表后，原会话从侧栏消失、停止状态丢失，却仍有后台进程运行的问题。
- 修复 Grok Build 原生会话 ID 切换期间运行锁未同步迁移，可能使同一 transcript 被重复发送，或停止命令无法准确命中当前回合的问题。

## 1.4.0 - 2026-08-21

### 新增

- 输入框新增 Claude Code 风格的“添加内容”菜单，可选择文件或图片、切换工作文件夹、导入 GitHub Issue，并直达连接器、插件和命令；完整 Issue URL 会按链接中的真实仓库导入。
- 队列消息支持原地编辑、保存或取消，并继续提供排序、立即发送、失败重试和删除；Codex、Claude、Grok 以及竞技场各分栏均保持独立队列。
- 欢迎页和设置弹窗新增本机真实用量概览、最长 182 天热力图与模型 donut 图；模型较多时自动聚合为“其他模型”，比例仍保持准确。

### 修改

- 设置页改为保留工作台状态的居中弹窗；模型商切换、用量、账户、插件、MCP、路由及环境配置统一在弹窗内完成。
- Composer 调整为更紧凑的分层布局，模型搜索和推理强度采用可拖动离散刻度；发送与停止共用同一图标位置，运行时显示有形变、光晕和 shimmer 的 Orb Loading。
- 移除每条消息最多四张图片的固定限制；图片在输入区和对话区均可预览、点击放大，并按单张 20 MB 的安全边界处理。
- 对话滚动改为每栏独立时间线，离开底部后以带进入/退出动画的“跳到最新”胶囊返回，不再使用突兀的侧边滚动轨道。

### 修复

- 修复长对话、双栏和三栏时整页滚动、输入框被挤出、各栏互相带动，以及首次向上滚动不显示返回入口的问题。
- 修复文件选择、图片选择或 GitHub Issue 导入期间切换会话时，异步结果写入错误草稿；过期仓库检测响应也不会再覆盖当前工作区。
- 修复粘贴或发送后的 data URL、本地路径及多种 provider 图片结构被误判，导致对话图片裂开、无法放大或重载后丢失的问题。
- 修复模型图表极端比例下扇区总和超过 100%，并消除设置弹窗引入的无效动态分包警告。

## 1.3.9 - 2026-08-20

### 新增

- 能力中心加入五类模型商原生上下文策略：Codex 可写窗口与自动压缩 Token 阈值，Claude 可设置客户端保护上限与压缩比例，Grok Build 可设置原生压缩比例，Gemini 可设置 `compressionThreshold`，OpenCode 可设置 auto / prune / reserved 及已声明模型的 context limit。
- Codex 原生配置保存后可在界面内安全重连 app-server；Claude、Grok、Gemini 与 OpenCode 明确显示“下一回合读取”，避免把每回合新进程误当成需要重启的常驻服务。
- 输入区加入运行时、工作区与 Git 分支胶囊，推理强度采用精确离散刻度拖动控件，并增加有形变与光晕的运行 Loading。

### 修改

- Composer 改为宽幅分层布局：正文输入、附件/权限工具、模型/推理设置各自分区；发送与停止在同一固定位置切换图标，不再显示独立停止按钮。
- 模型上下文按真实能力分级：Grok API 与 Gemini 固定为上游窗口，Claude 只允许缩小客户端上限，Grok/OpenCode 自定义窗口只作为压缩计算边界，防止误认为能扩展上游模型。
- Arena 分栏加入真实换位动画和细轨横向滚动条；窄栏自动收起权限与推理控件、缩短模型选择器，三栏及更多模型同时发送时不再挤压底部布局。

### 修复

- 修复粘贴、拖入或选择的图片只依赖临时路径，导致发送后、重载后或队列恢复后出现裂图；附件现复制到受控持久目录，输入区和消息区均可点击放大、失败重试。
- 修复 renderer 重载后队列遗留旧 `blockedByTurnId` 或本地已追加标记，导致 Codex、Claude、Grok 及复用 Codex 队列的 Gemini/OpenCode 永久等待或缺少用户消息的问题。
- 修复前端自行触发 Codex compact 与 CLI 原生自动压缩竞态；统一由各模型商原生配置负责压缩时机。
- 修复流式回复退化成纯文本、Markdown 排版在完成前后跳变，以及附件目录边界未解析符号链接的问题。

## 1.3.8 - 2026-08-20

### 新增

- 五类运行时统一增加配置检查、缓存重载和运行时重启入口，能力中心会显示配置文件、模型目录、权限模式、上下文来源以及“立即 / 下一回合 / 重连后生效”。
- Codex 增加可选上下文窗口覆盖与自动压缩阈值，达到阈值后使用原生 compact；Claude 保留原生 compact，Gemini / OpenCode 明确标记为本地历史摘要，Grok 明确不支持 compact。
- Composer 增加按厂商真实档位生成的推理强度滑杆、运行时 Queue Send 快捷按钮、轻量呼吸 Loading，以及图片加载失败重试和点击放大预览。
- 队列与草稿增加 renderer 恢复存储；模型竞技场为每栏保留独立草稿、队列、停止状态和附件，并支持窄屏横向滚动。

### 修改

- Send / Stop 合并为固定位置的主操作按钮；运行中输入新内容时单独显示加入排队按钮，停止当前任务不会清空草稿、图片或后续队列。
- 流式正文阶段改用轻量纯文本渲染，终态再启用完整 Markdown，减少长回复持续增量时的重复解析开销。
- Arena 两栏在空间充足时均分，三栏及以上保持每栏至少 360px；Gemini 不再展示或发送 CLI 不支持的 reasoning effort，OpenCode 权限只保留原生可映射项。

### 修复

- 修复 Grok 原生 `tool_call` / `tool_call_update` 未进入实时工具卡、磁盘快照覆盖实时工具，以及工具调用后已输出正文短暂重复的问题。
- 修复 Claude 使用非 Anthropic 兼容模型时注入上下文过大而触发 prompt too long，并保留必要文件与命令工具。
- 修复 Composer 临时 `blob:` 图片被远程 URL 安全策略拒绝、发送后提前回收导致裂图；消息图片改用持久路径和有界预览缓存，重开会话或 renderer reload 后仍可显示。
- 修复 SearchableSelect 同时使用 `defineModel` 与手工 emit 导致设置被提交两次，以及排队消息在 renderer reload 后残留 `sending` 状态无法继续的问题。

## 1.3.7 - 2026-08-19

### 新增

- Claude Code 会话支持原生手动 `/compact`：继承当前会话模型、推理档位和权限设置，并以 `compact_boundary` 结构化事件确认完成。
- Claude Code 自动/手动上下文压缩会显示为专用时间线事件，压缩后的上下文占用使用原生 `postTokens` 更新。

### 修改

- 五类运行时统一按“流式草稿 + 工具活动 + 原生历史终态”对账，文本段只由一个通道负责，工具与正文保持真实先后顺序。
- Gemini 默认不再强制过时的具体模型，优先继承 Gemini CLI 的 `GEMINI_MODEL` 或原生默认；用户明确选择模型时仍会传入 `--model`。

### 修复

- 修复 Claude Code 的增量、完整快照、最终结果和本地 transcript 同时合并时，同一 assistant 回复可能显示两次的问题。
- 修复 Claude Code `tool_result`、Task/后台通知、技能说明、系统提醒和压缩摘要等内部 user-shaped 记录被误显示为真人用户消息的问题。
- 修复 Grok 工具调用后，工具前正文因流式重放和空白归一化差异短暂复制到工具下方，随后又消失的问题。
- 修复 Gemini 配置中的旧默认模型覆盖 CLI 当前可用模型，导致 `model not found` 的问题。

## 1.3.6 - 2026-08-16

### 新增

- 官方 xAI Grok API 改为 Responses 接口，支持 `previous_response_id` 续聊和推理过程流式输出；自定义兼容网关仍走 Chat Completions。
- Codex / Claude / Gemini / OpenCode / Grok 失败提示改为口语化说明，不再直接甩 HTTP 状态码或 CLI 原文。
- 各运行时欢迎页提供各自的快捷建议，不再共用 Codex 文案。

### 修改

- 窄屏队列条也会显示下一条预览和失败条数。
- 思考过程展开后可复制；队列失败项可一键清除。
- Claude 设置里的权限模式改为中文/英文可读名称，输入栏不再露出 `acceptEdits` / `bypassPermissions`。
- 新安装 Grok API 默认模型改为 grok-4.6。

### 修复

- 同步 `app.ts` 版本回退值，避免界面仍显示旧版本号。

## 1.3.5 - 2026-08-16

### 新增

- 回合结束后可展开查看模型思考过程，不再只在流式阶段一闪而过。
- Grok 推理档位补齐官方 `xhigh`，默认模型目录加入 grok-4.6。

### 修改

- 队列条直接预览下一条待发送内容，并标出发送失败条数。
- Provider Router 不再把 401/403 当成可重试故障切换，避免错误密钥悄悄消耗下一个上游额度。

### 修复

- 修复 Grok API 流式只读 `delta.content`、丢弃 `reasoning_content`，导致思考过程不显示、用量里的 reasoning tokens 对不上官方 Chat Completions 格式的问题。
- 修复 Claude 代理后端把 `reasoning_content` 当成正文追加，思考内容混进回复的问题。
- 修复 Gemini CLI 官方 `tool_use` / `tool_result` 缺少 `tool_id`、`parameters`，以及 `error` / `result.status=error` 未按失败处理的问题。

## 1.3.4 - 2026-08-12

### 新增

- 为 Codex、OpenCode、Gemini、Grok 与 Claude Code 增加后端权威任务存活检查，在事件延迟或丢失时仍能恢复正确的运行状态。

### 修改

- 多栏会话改为稳定的同步布局，快捷会话下拉和左侧会话选择会明确作用于当前聚焦栏，切换时不再保留过渡中的旧栏目。
- 输入框改用 Vue 原生双向绑定，跨文件夹新建会话期间保持发送锁定，避免首次输入被吞或消息误发到旧会话。

### 修复

- 修复任务实际结束后仍显示“正在思考中”和停止按钮、队列无法继续接力，以及停止后重新发送缺少运行反馈的问题。
- 修复多栏会话串栏、切换闪烁、快捷下拉无反应、左侧会话无法切入聚焦栏和后台任务状态显示到其他栏的问题。
- 修复跨文件夹发送可能重复创建会话、同一后端会话被本地别名并发提交，以及事件重复关联造成的重复内容问题。
- 修复 Grok 与 Claude Code 临时任务 ID 和后端任务 ID 不一致导致的运行状态残留，并补齐运行映射的空值保护。

## 1.3.3 - 2026-08-11

### 新增

- Grok 与 Claude Code 增加真实后台回合存活检查；Codex、Gemini 与 OpenCode 增加权威会话快照复查，终止事件偶发丢失时也能自动识别任务已经结束。

### 修改

- 单栏、多栏、后台栏目和侧栏统一按具体会话与回合收敛运行状态；临时会话升级为真实会话时同步迁移结束状态复查，删除会话或销毁运行时会清理对应定时器。
- 后台状态查询采用延迟复查与二次确认，查询异常时继续重试，避免瞬时错误把仍在执行的任务误判为完成。

### 修复

- 修复任务实际上已经结束，但界面偶发长期停留在“正在思考中”、仍显示停止按钮并阻塞后续队列的问题，覆盖 Codex、Claude Code、Grok、Gemini 与 OpenCode。
- 修复会话从临时 ID 映射为真实 ID 后仍使用旧 ID 检查任务状态，导致后台栏目或切换会话后无法自动结束运行标记的问题。
- 修复旧回合的延迟完成事件可能影响同一会话中新回合状态的问题；恢复逻辑只处理原回合，不会清除后来开始的任务。

## 1.3.2 - 2026-08-10

### 新增

- Grok 每次发送都会生成稳定的前端回合标识，并由后端在开始、流式输出、工具活动、会话绑定和结束事件中原样返回，用于准确区分连续回合。

### 修改

- Grok 单栏与多栏统一按“栏目会话 + 前端回合 + 后端回合”关联运行状态；生成绑定补全 Claude/Grok 活动回合与 Grok 前端回合字段，前后端类型保持一致。
- 队列消息从等待转为执行时仍会重新进入对应会话时间线，并由该栏目的运行状态立即显示“正在思考中”。

### 修复

- 修复 Grok 暂停后立即再次发送时，旧回合的延迟开始、中断、文本、思考或工具事件可能清除新回合状态，导致单栏或多栏不显示“正在思考中”、看起来没有执行的问题。
- 修复上一条 Grok 消息结束时，下一条队列消息因后端尚在释放而暂缓后，重试沿用错误的“已加入时间线”标记，最终从队列和会话内容中同时消失的问题。
- 修复旧 Grok 结束事件晚于新回合到达时可能清空新回合流式内容、停止标记或错误占用队列的问题。

## 1.3.1 - 2026-08-10

### 新增

- 多栏队列面板会合并同一会话临时 ID 与真实 ID 下的消息，并按发送时间去重显示，栏目切换和会话晋升期间也能持续看到真实排队内容。

### 修改

- 多栏顶部的文件夹名称、Git 分支、变更数量和项目切换提示改为按栏目会话归属显示；跨文件夹切换只更新聚焦栏目，不再让其他栏目同步闪烁或借用当前文件夹状态。
- 已失败且等待手动重试的队列消息不再把已结束会话误判为忙碌，重命名、归档和删除等会话操作可正常使用。

### 修复

- 修复 Claude Code 后端在完成事件发出后才释放运行记录，前端立即重读时可能把已结束任务重新标记为运行中，导致长期停留在“正在思考中”的问题。
- 修复 Claude Code 临时会话升级为真实会话后，完成事件只清理精确 ID、遗漏别名下 active turn，进而阻塞后续队列续发的问题。
- 修复 Claude Code 非聚焦栏目错误依赖全局工作区，导致绑定其他文件夹的栏目发送按钮不可用或消息无法进入该栏目队列的问题。
- 修复 Grok 重复或乱序终止事件未释放遗留 sending/running 状态，以及旧回合未登记完成时可能持续阻塞后续排队消息的问题。

## 1.3.0 - 2026-08-09

### 新增

- 左侧最近会话列表的文件夹分组标题支持滚动吸顶；当前文件夹会持续显示在列表顶部，直到下一个文件夹标题将其顶走。

### 修改

- Codex、Gemini、OpenCode、Claude Code 与 Grok 统一使用相同的文件夹滚动定位体验，并保留原有折叠、拖拽排序、项目内新建和数量显示操作。
- 吸顶标题使用不透明侧栏背景与独立层级，避免滚动中的会话文字穿透标题，同时保持当前文件夹高亮和悬停反馈。

### 修复

- 修复会话数量较多时，滚动离开文件夹标题后无法判断当前正在浏览哪个文件夹的问题。

## 1.2.13 - 2026-08-09

### 新增

- Claude Code 与 Grok Build/API 增加后端会话工作区所有权校验；已有会话只能在其真实所属文件夹继续发送，前端状态异常时也不会跨目录执行。
- Claude Code 与 Grok 的 pending 首发使用“服务商 + 工作区 + 客户端会话 ID”生成稳定会话 UUID，相同首发请求无法再并行创建两条真实会话。

### 修改

- Codex、Gemini、OpenCode、Claude Code 与 Grok 的公共 Composer 统一串行处理首次发送，并在发送前固定栏目、服务商、会话和工作区归属。
- 竞技场恢复栏目时先读取未知会话的真实摘要，再校准服务商与工作区；Grok API 会话列表改为保留全部项目并将当前项目优先排序。
- 同一会话的焦点激活与后台时间线预加载不再重复竞争，切换栏目或文件夹时由最新选择唯一控制历史写入。

### 修复

- 修复首次发送等待创建会话时连续触发 Enter，导致同一消息创建两条新会话并同时出现在 A 文件夹的问题。
- 修复多栏切换 Codex、Gemini、OpenCode、Claude Code 或 Grok 会话后，栏目显示另一文件夹内容、消息不可见、错误排队或短暂闪烁的问题。
- 修复 Claude Code/Grok 会话摘要暂时未加载时回退到全局当前文件夹，以及 pending ID 晋升后历史读取可能把真实会话永久留在加载状态的问题。
- 修复已聚焦会话只有 session ID、尚无时间线缓存时被错误视为已打开，导致空白栏目不再主动恢复历史的问题。

## 1.2.12 - 2026-08-09

### 新增

- Composer 发送时会固定捕获目标会话、服务商运行时和工作区，排队、首次创建及后台发送始终使用同一组会话归属。

### 修改

- 工作区切换期间仅在目标会话及目录已经明确时允许发送；Claude Code、Grok、Codex、Gemini 与 OpenCode 均不再二次读取可能已变化的全局活动会话。
- pending 会话晋升为真实 UUID 后同步更新每个工作区的最后会话记忆，并忽略重启后已经失效的临时会话 ID。

### 修复

- 修复新会话首次发送后用户消息消失、时间线为空并错误显示“排队中 1”的问题。
- 修复从 A 文件夹发送后切换 B 文件夹继续发送时，消息可能写回 A、生成两条新会话或在两个会话显示相同内容的问题。
- 修复 Codex 首次分配真实 thread 时本地 UUID 与原生 thread ID 竞速形成重复侧栏记录，并收紧无工作区事件的 pending 会话认领。
- 修复 Gemini/OpenCode 仅在最终结果返回 native session ID 时，原生历史同步可能在绑定前导入第二条镜像会话的问题。

## 1.2.11 - 2026-08-09

### 新增

- 五个服务商的会话快照统一携带真实后台回合状态；切回仍在执行的 Codex、Claude Code 或 Grok 会话时，可继续显示并停止对应任务。
- 设置页已归档面板会随当前服务商自动刷新，Claude Code 与 Grok Build/API 的归档结果按请求目标隔离。

### 修改

- 竞技场多栏的侧栏、快捷会话、发送、停止、导航历史和会话操作统一按栏目、服务商、工作区及会话四层归属处理。
- Codex、Gemini、OpenCode、Claude Code 与 Grok 的异步会话打开和流事件采用目标会话校验，旧请求不再接管新焦点或覆盖新数据。
- Claude Code 原生 transcript 与本地稳定会话 ID 使用确定性绑定；历史重复记录继续保留，但侧栏只展示正确归属的一条会话。

### 修复

- 修复切换栏目、会话或文件夹时出现旧内容闪烁、错误运行状态、伪停止，以及任务实际仍在后台执行的问题。
- 修复 OpenCode 新对话可能出现两个会话、最终内容重复追加，以及跨服务商栏目显示错误会话的问题。
- 修复 Grok Build/API 快速切换时旧会话或归档请求覆盖当前 backend，并修复 Claude Code 归档刷新乱序的问题。
- 修复会话持久化文件内容为合法 JSON `null` 时产生 nil map，继而在读取或同步会话时触发 `assignment to entry in nil map` panic 的问题。
- 修复运行中会话仍可被归档或删除、pending ID 升级后栏目绑定丢失，以及模型列表旧请求覆盖当前服务商设置的问题。

## 1.2.10 - 2026-08-09

### 新增

- 竞技场快捷会话加入加载失败提示与原位重试；Codex、Gemini、OpenCode 会按明确的 runtime 和工作区加载、创建及恢复会话。
- 浏览前进/后退记录加入栏目、服务商、工作区和会话归属，竞技场中返回历史会回到原栏目，不再打开到当前全局会话。

### 修改

- 时间线切换改为稳定复用组件，不再按会话销毁重建或重复播放淡入位移动画；已有缓存或正在流式输出的内容在后台刷新时保持可见。
- 运行、发送、停止、加载、队列、审批、回滚和会话变更统一按目标 session 查询，并兼容 pending 会话升级后的 ID 别名。
- 左侧会话列表和栏目快捷下拉统一跟随当前聚焦栏目；跨文件夹会话也能正确高亮并显示真实后台运行状态。

### 修复

- 修复切换会话或文件夹后，运行中的旧会话界面显示已停止、实际后台仍继续执行，以及新会话短暂借用旧会话停止按钮的问题。
- 修复项目列表或历史快照中的旧 `idle` 状态覆盖内存真实运行状态，并在恢复期间提前结算流式条目造成的假停止。
- 修复切换栏目时旧时间线、加载骨架和非聚焦栏透明度变化造成的闪烁，以及快捷会话触发器偶发无法打开的问题。
- 修复侧栏严格比较旧/新 session ID 导致 Codex、Claude、Grok 后台运行圆点消失，或操作错误会话的问题。
- 修复竞技场中的新建、首次发送、工作区选择、命令面板、标题栏、空状态、恢复、compact、fork、rollback 等入口仍可能使用全局活动会话的问题。
- 修复后端读取、发送、恢复、归档、删除和重命名会话时可能按当前文件夹误路由，以及 `ReadThread` 写入未初始化映射导致 panic 的问题。

## 1.2.9 - 2026-08-09

### 新增

- 同服务商多栏允许直接选择另一栏正在查看的会话；选择后自动交换两栏会话，既保持每栏独立，也不再出现无法点击的禁用项。

### 修改

- 栏目快捷会话改为先即时绑定栏位、再统一激活运行时和加载历史；侧边栏高亮同步显示当前聚焦栏实际绑定的会话。
- 左侧新建会话、项目内新建和切换项目统一作用于当前聚焦栏，并保留其他栏尚未发送的独立草稿。

### 修复

- 修复新增栏目后快捷会话下拉选择没有反应，以及已在其他栏的会话选项无法点击的问题。
- 修复竞技场中点击左侧会话只改变全局会话、没有切换当前栏目内容的问题。
- 修复快速切换栏目、运行时或侧边栏会话时，较早的异步打开请求晚返回并覆盖最新选择的问题。
- 修复竞技场切换运行时后自动恢复全局记忆会话，导致栏位绑定与全局活动会话再次错位的问题。

## 1.2.8 - 2026-08-09

### 新增

- 模型竞技场加入栏位会话所有权校验：Codex、Gemini 与 OpenCode 共用时间线存储时，仍会按栏位 runtime 校验真实会话归属。

### 修改

- 新增或复制模型栏时默认保持独立空会话，不再猜测并继承全局活动会话；首次打开竞技场时仅为首栏显式保留原来的当前会话。
- 时间线、发送目标、停止操作、消息队列、上下文用量与会话命令统一改为使用当前栏绑定的 session，而不是共享的全局 active 指针。

### 修复

- 修复 Codex 对话进行中新增 OpenCode 等栏位后，新栏错误显示 Codex 当前会话、消息可能发入错误会话的严重串栏问题。
- 修复同厂商多栏可能复用另一栏未发送的 pending 会话，以及旧版本遗留的跨厂商错误绑定继续污染栏位的问题。
- 修复非聚焦栏错误显示其他栏的思考状态、历史分页、Token 上下文和队列状态的问题。

## 1.2.7 - 2026-08-08

### 新增

- **模型竞技场分栏**：右键厂商图标可开启 2–8 栏并排对比；支持同厂商多栏、拖拽实时换位、复制栏、加栏/减栏。
- 每栏独立绑定厂商与会话；快捷会话下拉按项目分组，带厂商/文件夹图标、长文本省略与组件 Tooltip。

### 修改

- 会话选择器与全局 Select 触发器优化：宽触发器不再把菜单拉满整页，长标签可正确省略。
- 侧边栏去掉「右键厂商图标可打开竞技场分栏」常驻提示文案（入口仍保留右键菜单）。

### 修复

- 竞技场各栏会话与时间线互不抢焦点；Grok 非全局活动会话也能按栏展示历史与运行中状态。
- 同厂商多栏禁止绑定同一会话，避免对话串栏。

## 1.2.6 - 2026-08-08

### 新增

- 设置 → 环境：新增 **网络代理**，可对接 Clash / 同类工具的 HTTP 混合端口（如 `http://127.0.0.1:7890`），无需开启 TUN。
- 启用后向 NiceCodex 进程注入 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY`，Codex、Claude、Grok、Gemini、OpenCode 等子进程 CLI 与 Go 出站请求会继承该配置。
- 提供常用端口预设（7890 / 7897 / 10809）与 `NO_PROXY` 排除列表（默认 localhost）。

## 1.2.5 - 2026-08-08

### 修复

- 修复 `pendingCodexSessionID` 单槽竞态：同一工作区并发首个分配时，迟到的 `thread/started` 事件可能把线程绑定到错误的会话。现改为按工作区映射并增加后端线程所有权校验。
- 修复 OpenCode JSON part 快照完全重排时整段重复追加的问题，改为按最长公共前缀只追加新增内容。
- 修复前端线程 `createdAt/updatedAt` 时间单位不一致（浮点秒混入整数 Unix 秒）。
- 修复本地 Provider 路由熔断计数不清零、冷却窗口度量可能失真；半开探测失败时重置连续失败计数。
- 修复 `codex.Client.Stop()` 中 `time.After` 定时器在优雅退出分支下泄漏的问题。

## 1.2.4 - 2026-08-07

### 修复

- 修复 OpenCode、Claude Code、Grok 和 Codex 用量回填沿用旧错误桶、日期范围显示相同的问题。
- OpenCode 用量改为从原生 SQLite 消息按本地日期重建日桶，Codex 回溯改为扫描全部 active/archived rollout。
- 修复 OpenCode JSON part 快照重复追加；保留 CLI JSON 模式的原生分段输出语义。

## 1.2.3 - 2026-08-07

### 修复

- 修复 OpenCode 新建对话首次发送时本地会话与原生 session 被重复显示的问题，并在原生 session id 出现后立即完成绑定。
- 修复打开 OpenCode 原生会话时合并 token 用量可能写入 nil map，导致 `ReadThread` panic 的问题。

## 1.2.2 - 2026-08-07

### 修复

- 修复 OpenCode provider/model 下拉为空、CLI shim 识别失败和原生配置回退不完整的问题。
- 修复 Codex 详细 Token 用量被总数回退接口覆盖的问题，并统一 external runtime 的累计用量口径。
- 修复 `/usage`、`/add`、`/mention` 等 slash 命令无响应或参数静默发送的问题。
- 修复 Codex 自动上下文压缩完成后标准 `contextCompaction` 事件未刷新线程和用量状态的问题。

## 1.2.1 - 2026-08-06

### 新增

- Gemini CLI 与 OpenCode 的原生 usage 统计接入用量面板，支持输入、缓存、输出、推理和模型分组数据。
- 外部运行时的模型、推理、沙箱和审批设置继续使用各自独立字段，并保留 provider/model 原生信息。

### 修改

- Codex 项目和会话切换复用工作区请求与历史快照，延迟后台刷新，避免长任务期间切换文件夹变慢或旧结果覆盖新会话。
- Codex 历史缓存、只读线程快照和队列恢复逻辑增强，切换回来时保留正在执行的回合、流式内容和待发送消息。
- 外部 CLI 的 usage/catalog 探测使用无窗口托管进程，Windows 下不会弹出空 PowerShell 窗口；usage 缓存按 runtime 与 workspace 隔离。
- Gemini/OpenCode 用量读取改为账号范围聚合；OpenCode CLI 不可用时直接从原生 SQLite 数据库读取，Gemini/OpenCode 原生 token 字段也会被识别。
- Gemini/OpenCode 侧栏现在显示与其他运行时一致的 token 用量入口、范围统计和输入/缓存/输出/推理明细。
- 长会话切换提高前后端历史缓存容量，并复用已加载的 Codex 时间线，减少长对话切换时的重复读盘、反序列化和 WebView2 CPU 峰值。
- 侧边栏会话列表按真实 provider 使用 Claude、Grok、Gemini、OpenCode 和 Codex 图标。

### 修复

- 修复 Claude 事件监听器在重复 bootstrap 后累积的问题，dispose 时现在会可靠解绑。
- 修复外部 token 事件缺少 runtime 导致 Gemini/OpenCode 用量被错误归入 Codex 的问题。
- 修复 Gemini 原生 `totalTokenCount` 与 cached token 重复计数，以及 OpenCode cache read 未纳入总量的问题。
- 修复切换 Gemini/OpenCode 模型时污染 Codex 模型目录、会话协作模式和权限设置的问题。

## 1.2.0 - 2026-08-06

### 修改

- Codex 项目切换复用健康的 app-server，并通过每次请求的工作区路径访问线程；设置页的显式重连会先停止再启动进程以应用最新客户端配置。
- 项目线程列表刷新改为携带明确工作区路径，避免快速切换时旧项目的异步请求查询到新项目并覆盖侧边栏状态。

### 修复

- 修复切换项目或新建项目会话时触发连接检查，重启另一个项目正在运行的 Codex app-server，导致原项目任务中断的问题。
- 修复并发启动/停止 Codex 的生命周期竞态，避免切换项目期间出现偶发断连和任务状态丢失。
- 会话正文继续使用按线程缓存；复用连接和切换项目不会清空已加载历史，只有进程真正重启或缓存淘汰时才重新读取。

## 1.1.9 - 2026-08-06

### 新增

- Gemini CLI 与 OpenCode 改为独立的原生运行时 catalog：分别读取各自的 provider、模型、配置来源、MCP、instructions、原生历史与 usage，不再把 Codex 的服务商字段当作外部运行时配置。
- OpenCode 完整读取 `opencode models --verbose`，按 provider 展示 OpenCode Zen、OpenCode Go 及已发现的第三方 provider/model，并支持独立选择 provider、添加自定义原生模型。
- Gemini/OpenCode 能力中心支持查看并保存各自的 MCP JSON 与 global/project instructions；设置页支持独立自定义模型入口。
- OpenCode usage 从原生 session 数据库聚合 token、cache、reasoning、cost 与 model 分组；Gemini usage 从原生 chat JSONL 的 usage metadata 聚合。
- 原生 MCP JSON 编辑会保留未展示的 env、headers 和 transport 字段，避免保存时丢失 CLI 配置；Windows 配置文件写入使用可恢复的备份替换。
- Gemini/OpenCode MCP 现在区分 global 与 project 配置，项目级服务不会再被全局配置覆盖或漏读。

### 修改

- 外部运行时历史读取按 workspace 与 CLI 原生存储隔离：Gemini 使用 `~/.gemini/tmp/*/chats/*.jsonl`，OpenCode 使用 `opencode db` 查询自己的 session 数据库。
- OpenCode 发送模型保持 `provider/model` 原生格式，Gemini 继续使用 Gemini CLI 的 `--model`；外部 instructions 不再复用 Codex `customInstructions`。
- OpenCode 当前 provider 优先从原生 `model` 前缀识别；项目指令兼容 `.opencode/AGENTS.md`，Gemini 历史跳过 CLI 注入的 session context 并正确按 30 天读取 token metadata。

### 修复

- 修复 Gemini/OpenCode 只有 fallback 模型、没有真实 provider/自定义模型、MCP/提示词/历史/usage 被错误标注为 Codex 配置的问题。

## 1.1.8 - 2026-08-06

### 新增

- 左侧运行时新增 Gemini CLI 与 OpenCode，支持官方品牌图标、Tooltip、窄屏图标模式、独立模型/推理设置和 provider 状态识别。
- Gemini/OpenCode 接入会话创建、历史、发送、停止、归档、重命名和会话级 FIFO 队列，并按 provider 隔离项目会话。
- Capabilities 与 Settings 增加 Gemini/OpenCode 独立运行时入口，并明确各自原生 MCP 与 instructions 配置来源。

### 修复

- 修复 MCP 粘贴 JSON 在 Wails WebView 中没有写回输入状态、导致文本不显示和预览不触发的问题；粘贴时保留光标位置并继续执行长度校验。
- 扩展 OpenCode JSON 流解析，兼容 `part` 嵌套文本和思维事件，避免不同 CLI 版本的消息内容丢失。
- 外部运行时读取会话时跳过 Codex app-server 探测，避免切换 Gemini/OpenCode 时产生无意义的连接等待。
- 修正外部会话的压缩、回滚和 `/review` 命令边界；Gemini/OpenCode/Claude 仅显示实际支持的会话操作，避免误报成功。

## 1.1.7 - 2026-08-06

### 修复

- 修复 Grok 继续会话发送新消息后，上一轮 AI 回复会在新用户消息下方短暂重复，随后才替换为“正在思考中”的问题；活动轮询现在按本轮启动前的历史文件边界识别当前用户消息，前端同时过滤迟到、跨回合和已存在于历史中的活动快照，并兼容长提示词及连续相同提示词。

## 1.1.6 - 2026-08-05

### 新增

- 会话历史加载超过 8 秒时显示安全的“重新加载当前会话”操作，可让过期读取请求失效并重新获取当前历史，不会停止正在运行的任务，也不会清除本地消息或排队内容。
- 路由与服务商设置现在直接读取 Codex `config.toml` 和 Claude Code `settings.json`，展示两个 CLI 当前实际使用的 provider、名称、Base URL、来源与配置路径，同时不暴露 API Key。
- MCP 导入提供明确的“配置文件 / 粘贴 JSON”双入口，设置顶部可直接进入可编辑 JSON 模式，原有可视化校验、预览和原子写入保持不变。

### 修改

- 恢复“排队下一次 / 追加到当前回合”跟进行为选择；追加仅在真实会话、真实运行回合且没有加载、提交、审批或已有队列时生效，任何不确定状态仍自动进入会话级 FIFO。
- CLI 一键安装与更新统一使用 pnpm，并在 GUI 进程中自动创建和注入 `PNPM_HOME` 及全局 bin 路径，新安装的 CLI 无需等待重启即可重新检测。

### 修复

- 修复 Codex、Claude Code 与 Grok 在新会话运行中跨项目快速切换时，旧历史请求占住加载屏障并长时间停留在“正在加载对话”的问题；三种运行时均可使当前会话的旧请求失效后重新加载。
- 修复旧会话容器的延迟滚动事件覆盖新会话“跟随底部”状态，导致切换完成后停在上次阅读位置的问题；现在切换、加载完成和发送消息后都会稳定定位到最新内容。
- 修复 Windows 图形进程未继承终端 pnpm 配置时，一键更新报 `ERR_PNPM_NO_GLOBAL_BIN_DIR` 的问题。

## 1.1.5 - 2026-08-05

### 新增

- 设置新增 MCP 独立入口与可视化配置，可逐项编辑本地命令、参数、环境变量和远程 URL，并支持文件拖放、文件选择或直接粘贴 JSON 导入；导入前提供预览、校验与删除确认，批量写入保持原子性。
- 设置新增仅监听 `127.0.0.1` 的本地服务商路由，可按顺序自动故障切换，并提供关闭、开启和半开状态的熔断恢复、运行状态及一键应用/恢复 Codex 配置。

### 修改

- Codex、Claude Code 与 Grok 的普通发送统一先进入会话级 FIFO，旧电脑持久化的 `steer` 行为自动迁移为队列，避免不同 CLI 版本或事件时序导致运行中消息直接发送。
- 长会话历史改为有总量上限的快照与 LRU 缓存；活跃、加载、运行和排队会话受到保护，同时限制终态集合与流式状态，降低历史读取、内存占用和频繁切换成本。
- 文件修改列表改用应用内 Tooltip 展示完整绝对路径，行内路径优先保留盘符并对中间目录省略，替代原生浏览器提示。

### 修复

- 修复 Codex、Claude Code 与 Grok 在会话加载、恢复、发送 RPC 和终态事件乱序时出现消息消失、重复、越过队列、错误发送到其他会话或错误停止其他项目任务的问题。
- 修复 Grok 长消息在磁盘历史被截短后发生重复或问答顺序错乱、流式内容与工具历史合并不稳定，以及文件已修改却显示 `+0 -0` 的问题。
- 修复本地服务商路由保存失败时运行时、配置文件和 Codex 配置不能完整回滚，以及配置更新后旧请求污染新熔断状态、取消半开探测导致服务商永久不可用的问题；同时保护 API Key 并拦截跨站浏览器请求。
- 修复设置面板入口切换异常，以及超大 MCP JSON 直接粘贴可能导致界面卡顿和内存增长的问题。

## 1.1.4 - 2026-08-05

### 新增

- Codex、Claude Code 与 Grok 长会话改为分批加载；滚动到对话顶部可继续加载更早回合，并保持当前阅读位置。
- 每个运行时、项目和会话分别保留未发送的文字与图片草稿，来回切换后可继续编辑。
- 输入框支持使用 `↑` / `↓` 浏览当前会话已发送消息，并可按 `Esc` 恢复浏览前草稿。

### 修改

- 回合末尾的文件修改列表默认收起，按需展开后仍显示完整文件与增删行统计。
- Codex 默认模型统一为 `gpt-5.6-sol`，空的旧配置自动归一，同时保留用户显式选择并兼容模型 ID 大小写差异。
- Grok 超大 `updates.jsonl` 改为从文件尾部读取近期 token 使用记录，减少长会话打开时的磁盘读取开销。

### 修复

- 修复异步添加图片期间切换会话后附件落入当前会话、并发添加可能超过上限，以及本地图片预览未及时释放的问题。
- 修复发送失败恢复原消息时覆盖用户已经输入的新草稿，或 pending 会话升级真实 ID 时覆盖目标草稿的问题。
- 修复 Claude Code 停止操作可被连续触发，并防止 Claude Code/Grok 上一回合迟到的中断结果清理新回合状态。

## 1.1.3 - 2026-08-04

### 修改

- Codex 发送按钮、队列提示和实际调度统一使用完整的会话忙碌状态，加载、提交、运行、重试与服务端活跃状态保持一致。

### 修复

- 修复 Codex 在切换会话并于历史加载期间发送首条消息后，运行中的后续消息可能消失、绕过队列直接发送或重复展示的问题。
- 修复 `ResumeThread` 发现已有活跃回合时仍继续调用 `SendMessage`，现在会恢复真实回合所有权并将消息稳定停回原会话队列。
- 修复无 `turnId` 错误、迟到的 idle 对账、会话 ID 别名及断线重连错误释放新提交状态，避免不同机器事件时序差异造成错误出队。

## 1.1.2 - 2026-08-03

### 修复

- 修复 Codex 在切换会话或加载历史期间发送首条排队消息后，发送 RPC 与 `turn/started`、`turn/completed`、`turn/error` 事件乱序造成的短暂假空闲，避免后续消息消失、绕过队列直接发送或重复执行。
- 修复 Codex pending 会话升级为真实会话 ID、断线重连或旧 RPC 延迟返回时提交锁被错误释放，并同步迁移队列阻塞关系、协作模式和会话偏好写入状态。
- 修复 Codex 历史快照覆盖正在提交的本地用户消息、相同文本消息被错误合并、用户消息出现在回答下方，以及活跃队列会话被缓存清理的问题。

## 1.1.1 - 2026-08-03

### 新增

- 左侧项目支持拖拽或使用上下方向键自主排序，Codex、Claude Code 与 Grok 分别保存项目顺序，重启后继续生效。

### 修改

- 点击或使用项目后不再把项目自动提升到列表顶部；已有项目保持用户顺序，新项目追加到最近项目列表末尾。
- 用户发送消息后自动回到底部，后续流式更新仍尊重用户主动向上滚动的阅读位置。
- 三种运行时统一以“目标会话加载屏障 + 会话级 FIFO 队列”处理发送，Claude Code 与 Grok 的队列项固定使用入队时的项目、backend、模型和思维链级别。

### 修复

- 修复项目或会话仍在加载时发送消息可能落入旧会话、绕过队列、短暂消失、重复展示或被旧历史响应覆盖的问题，并覆盖快速 A→B→A 切换场景。
- 修复项目切换期间停止按钮可能停止上一个项目任务，以及项目列表加载完成后覆盖用户刚创建会话的问题。
- 修复 Grok 已收到 `turn.started` 事件但发送调用稍后报错时删除用户消息，造成“正在思考时消息消失、稍后又出现”及重试重复执行的问题。
- 修复 Grok 长提示词的原生历史只保存前缀或后缀时用户消息重复、顺序错乱，以及历史暂时不可读时队列永久停留的问题。
- 修复 Grok/Claude Code 队列首项失败后后续消息越过它发送、CLI 已结束但本地仍被孤立回合锁住，以及删除或归档后残留队列和加载状态的问题。
- 修复 Claude Code 连续发送相同文本时被错误去重、pending 会话映射时队列被覆盖，以及本地用户消息遗漏图片附件的问题。
- 修复 Codex 历史读取与实时事件并发时覆盖新用户消息或流式内容、切换项目中途留下 `submitting` 状态，以及无目标项目加载期间仍可误发到旧线程的问题。
- 修复切换项目或 pending 会话升级真实 ID 时误清空用户刚输入但尚未发送的下一条草稿。

## 1.1.0 - 2026-08-01

### 修改

- Codex、Claude Code 与 Grok 的运行中发送统一采用会话级队列屏障，确保终态事件、状态刷新和连续发送并发时仍按 FIFO 顺序执行。
- Grok 新会话使用多级会话别名归并本地状态、流式内容、任务指标与待发送队列，项目或会话切换后仍由原会话独立管理任务。

### 修复

- 修复 Grok 新建对话发送长消息后切换会话可能误报 session 不存在、用户消息短暂消失、重复上一条消息或用户消息与回答顺序颠倒的问题。
- 修复 Grok 极快完成、停止轮询或发送握手失败时 pending turn 未正确终结，导致任务状态复活、后续消息卡住或偶尔绕过队列的问题。
- 修复 Grok 思考流短暂显示 `clear,,,` 控制文本，以及文件已实际编辑却显示 `+0 -0` 的问题。
- 修复切换项目后 Grok 停止按钮可能暂停其他项目任务，以及后台会话事件错误写入当前会话的问题。
- 修复 Claude Code 后台事件误绑定当前 pending 会话、终态后任务重新变为运行中，以及删除失败队列项后后续消息无法继续的问题。
- 修复 Codex 会话选择 `max` 思维链后偶尔回落为 `high` 的问题；模型、思维链级别与协作模式现在按会话串行保存并在恢复后回填。

## 1.0.9 - 2026-07-31

### 修改

- Grok 工作区进入时改用本地快速运行时检测，避免自动执行 `grok models` 网络鉴权探测与正式消息争抢初始化资源。
- Grok Build 新会话跳过无意义的原生会话全量查找，工具历史轮询等待首个有效周期后再读取，缩短消息开始处理前的等待。

### 修复

- 修复 Grok 发送长消息后，磁盘历史与本地乐观消息合并失败导致回答显示在上、用户消息显示在下的问题。
- 修复 Grok 持久化长文本存在换行或截断差异时无法识别同一条消息的问题，并保留本地完整用户内容。

## 1.0.8 - 2026-07-31

### 新增

- Grok API 模式支持随消息发送本地图片，并增加图片白名单、格式与大小校验。
- Grok 运行任务加入后端状态确认，停止操作会等待真实进程退出后再释放会话和发送队列。

### 修改

- Grok Build 的 sandbox、审批策略、Web 搜索和 X 搜索设置现在会映射到对应 CLI 参数。
- Grok API Base URL 增加 HTTPS 校验，仅允许 localhost 使用 HTTP；API Key 不再错误回退到 `OPENAI_API_KEY`。
- Grok API 会话、归档索引和原生会话标题改为安全替换写入，并在持久化失败时保留或恢复原状态。
- 会话搜索增加防抖，运行中 Grok 会话禁止归档和删除，防休眠状态覆盖 Codex、Grok 与 Claude 任务。

### 修复

- 修复 Grok 新会话发送长消息后切换会话，再切回时误报 session 不存在、重复显示上一条消息或丢失流式状态的问题。
- 修复同时运行多个项目或切换 Grok backend 时，停止、流式事件和后续队列可能串到其他会话、项目或 backend 的问题。
- 修复 Grok 发送前短暂显示 `clear,,,` 控制文本，以及终止事件早于发送响应时会话被错误恢复为运行中的竞态。
- 修复消息发送同步失败后乐观消息残留、失败队列被自动重复发送、会话列表加载失败时已有列表被清空的问题。

## 1.0.7 - 2026-07-30

### 修复

- 修复 Grok 发送新消息时，上一轮 reasoning 末尾文本会在 token 信息上方短暂重新显示扫光的问题。
- 修复 Grok 文件编辑已产生真实差异却显示 `+0 -0` 的问题，并保留原生工具调用中的文件 diff。
- 同步收紧 Claude 的流式消息分组判定，避免运行态切换期间错误激活上一轮已完成消息。

## 1.0.6 - 2026-07-29

### 新增

- 新增 `update.md`，统一记录每个版本的新增功能、行为调整和问题修复。

### 修改

- 桌面端移除左侧边栏右上角多余的圆形关闭控件；移动端继续保留关闭入口。
- Codex 会话状态兼容字符串和对象两种协议格式，避免不同 app-server 版本产生错误的忙碌状态判断。

### 修复

- 修复 Codex 长任务执行期间后续消息偶尔绕过队列、直接并行发送的问题。
- 修复读取线程状态失败或上游返回异常后，本地过早释放运行中回合，导致下一条消息错误发送的问题。
- 修复连续点击“新建任务”可能生成多个空白草稿会话的问题。
- 修复首条消息创建 Codex 后端线程时，并发请求可能创建两个后端会话的问题。
- 修复 `thread/started` 通知早于 `thread/start` 响应时，侧边栏同时显示 Nice Codex 会话和原始 Codex 会话的问题。
